// export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || window.location.origin; // default to current host
export const API_BASE_URL = window.location.origin; // Use current host to leverage Vite proxy

